export type User = {
  id: string,
  name: string,
  avatar_url: string,
};

export type Chat = {
  id: string,
  title: string,
  img_url: string,
  last_message: string,
};

export type Message = {
  id: string,
  text?: string,
};
